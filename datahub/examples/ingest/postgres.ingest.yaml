source:
    type: postgres
    config:
        include_tables: true
        database: datahub_test
        password: '${POSTGRES_PASSWORD}'
        profiling:
            enabled: false
        host_port: '172.17.0.1:5432'
        include_views: true
        stateful_ingestion:
            enabled: true
        username: '${POSTGRES_USERNAME}'
pipeline_name: postgres
