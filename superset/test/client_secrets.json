{
  "web": {
    "issuer": "$(KEYCLOAK_ISSUER_URL)",
    "auth_uri": "$(KEYCLOAK_AUTH_URL)",
    "client_id": "superset",
    "client_secret": "$(KEYCLOAK_CLIENT_SECRET)",
    "redirect_urls": ["*"],
    "userinfo_uri": "$(KEYCLOAK_USERINFO_URL)",
    "token_uri": "$(KEYCLOAK_TOKEN_URL)",
    "token_introspection_uri": "$(KEYCLOAK_INTROSPECT_URL)"
  }
}
